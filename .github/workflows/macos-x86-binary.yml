name: macos-x86-binary

on:
  workflow_dispatch:

jobs:
  build-macos-x86-binary:
    runs-on: macos-15-intel

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Verify architecture
        run: |
          set -e
          echo "Runner architecture: $(uname -m)"
          if [ "$(uname -m)" != "x86_64" ]; then
            echo "Expected x86_64 runner for macOS x86 binary build"
            exit 1
          fi

      - name: Build ztm binary
        run: |
          set -e
          ./build/deps.sh
          ./build/pipy.sh
          ls -lh bin/ztm

      - name: Package binary artifact
        run: |
          set -e
          mkdir -p dist
          cp bin/ztm dist/ztm
          tar -czf dist/ztm-macos-x86_64.tar.gz -C dist ztm

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ztm-macos-x86_64
          path: |
            dist/ztm
            dist/ztm-macos-x86_64.tar.gz
